<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>จัดการสินค้า</title>
    <link rel="stylesheet" href="/css/manage.css">
</head>
<body>
    <div class="container-products">
        <div class="product-table">
            <table>
                <thead>
                    <tr>
                        <th>ชื่อสินค้า</th>
                        <th>ราคา</th>
                        <th>แก้ไขสินค้า</th>
                        <th>ลบสินค้า</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- ข้อมูลสินค้าจะถูกเติมที่นี่ -->
                    <% Object.values(products).forEach(function (item) {%>
                        <tr>
                            <td><%= item.nameproduct %></td>
                            <td><%= item.price %> บาท</td>
                            <td>
                                <form action="/editproduct" method="POST">
                                    <input type="hidden" name="edit_id" value="<%= item._id %>">
                                    <button type="submit" class="edit-button" >แก้ไข</button>
                                </form>
                            </td>
                            <td><a class="delete-button" href="/delete/<%= item._id %>" onclick="return confirm('ต้องการรับข้อมูลนี้ไหม !')">ลบ</a></td>
                        </tr> 
                    <% }) %>

                </tbody>
            </table>
        </div>
    </div>
</body>
</html>